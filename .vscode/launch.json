{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "BackendApplication",
      "request": "launch",
      "mainClass": "com.boulderscoring.BackendApplication",
      "projectName": "backend",
      // Ohne das startet VS Code im Repo-Root und spring.docker.compose.file
      // (../compose.yaml, relativ zu backend/) zeigt ins Leere.
      "cwd": "${workspaceFolder}/backend"
    }
  ]
}
